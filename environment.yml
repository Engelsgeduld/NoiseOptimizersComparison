name: opims_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - numpy
  - pandas
  - scikit-learn
  - pytorch
  - matplotlib
  - jupyterlab
  - mlflow
  - pip
  - pip:
      - ruff
      - mypy
      - colorednoise
      - darts[torch]
